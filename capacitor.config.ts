import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.anusha.dic50analyser',
  appName: 'DIC50 Well Analyser',
  webDir: 'dist'
};

export default config;
